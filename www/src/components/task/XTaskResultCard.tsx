import * as React from "react";
import { Task } from "../../graphql/models";

const XTaskResultCard: React.FC<{ targetName: string; tasks: Task[] }> = ({
  targetName,
  tasks
}) => <React.Fragment />;
export default XTaskResultCard;
